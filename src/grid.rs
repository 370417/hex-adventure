struct Grid<T> {
    width: u32,
    height: u32,
    grid: Vec<T>,
}

struct Pos {
    x: i32,
    y: i32,
}

struct Displacement {
    x: i32,
    y: i32,
}

enum Direction {
    
}

impl <T> Grid<T> {

}
